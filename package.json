{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "npx nuxi build --dotenv .env.production",
    "test": "npx nuxi build --dotenv .env.test",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@nuxtjs/tailwindcss": "^6.7.0",
    "@types/node": "^18",
    "nuxt": "^3.5.2",
    "nuxt-simple-sitemap": "^2.7.0"
  },
  "dependencies": {
    "@nuxtjs/robots": "^3.0.0",
    "nuxt-swiper": "^1.1.0"
  }
}
